<h1>MY PROFILE</h1>

<table>
  <thead>
  <tr>
    <th>Email</th>
    <th>Name</th>
  </tr>
  </thead>

  <tbody>
  <% superadmin = current_superadmin %>
      <tr>
        <td><%= superadmin.email %></td>
        <td><%= superadmin.name %></td>
      </tr>
  </tbody>
</table>
<br>
<table>
  <td><%= button_to 'Manage Admins', admins_path, :method => :get %></td>
  <td><%= button_to 'Manage Superadmins', superadmins_path, :method => :get%></td>
  <td><%= button_to 'Manage Customers', customers_path, :method => :get %></td>
  <td><%= button_to 'Manage Cars', cars_path, :method => :get %></td>
  <td><%= button_to 'Manage Suggestions', suggestions_path, :method => :get %></td>
  <td><%= button_to 'Edit', edit_admin_path(admin), :method => :get %></td>
</table>
<br>
<br>